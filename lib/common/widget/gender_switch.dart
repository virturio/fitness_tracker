import 'package:flutter/material.dart';

class GenderToggleItem extends StatelessWidget {
  const GenderToggleItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
